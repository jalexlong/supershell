**Description:** A "dungeon." A secure server that blocks traffic.

**Game-play:** The first _real_ challenge. The player must use skills (maybe `grep` on logs, or `ssh` with a specific port) to get through.
